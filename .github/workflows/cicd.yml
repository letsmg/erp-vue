name: 🚀 Build & Deploy ERP Monolito Atômico

on:
  push:
    branches: [main]

jobs:
  build-and-pack:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐘 Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, bcmath, gd, zip, pgsql

      - name: 📦 Install Composer
        run: composer install --no-dev --optimize-autoloader --no-interaction

      - name: 📝 Prepare Build Env
        run: |
          cp .env.example .env
          php artisan key:generate

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: 🔨 Build Vite Assets
        run: |
          npm install
          npm run build

      - name: 🤐 Criar Pacote de Deploy
        run: zip -r deploy.zip . -x ".git/*" "node_modules/*" ".github/*" "tests/*" ".env*" "storage/*"

      - name: 🔑 Configure SSH & Known Hosts
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          known_hosts: ${{ secrets.SSH_KNOWN_HOSTS }}

      - name: 🚀 Deploy Atômico (Zero Downtime)
        run: |
          scp deploy.zip ubuntu@${{ secrets.SERVER_IP }}:/tmp/erp_monolito_deploy.zip

          ssh ubuntu@${{ secrets.SERVER_IP }} << 'EOF'
            set -e
            
            # CONFIGURAÇÃO DE CAMINHOS DO MONOLITO
            BASE_DIR="/var/www/erp"
            
            # 1. Preparar pasta temporária
            sudo rm -rf ${BASE_DIR}_new
            sudo mkdir -p ${BASE_DIR}_new
            sudo unzip -o /tmp/erp_monolito_deploy.zip -d ${BASE_DIR}_new
            
            # 2. AJUSTE DE PERMISSÕES ANTECIPADO
            # Garante que o usuário www-data possa criar o symlink do storage
            sudo chown -R www-data:www-data ${BASE_DIR}_new
            
            # 3. Linkar arquivos persistentes (Shared)
            sudo ln -sfn ${BASE_DIR}_shared/.env ${BASE_DIR}_new/.env
            sudo ln -sfn ${BASE_DIR}_shared/storage ${BASE_DIR}_new/storage
            
            # 4. Comandos Laravel (Rodando como www-data)
            cd ${BASE_DIR}_new
            
            # Remove link ou pasta storage residual do zip
            sudo rm -rf public/storage
            
            sudo -u www-data php artisan migrate --force
            sudo -u www-data php artisan db:seed --force
            sudo -u www-data php artisan storage:link
            sudo -u www-data php artisan optimize
            
            # 5. SWAP ATÔMICO (Ativa a nova versão)
            sudo ln -sfn ${BASE_DIR}_new ${BASE_DIR}_live
            
            # 6. Finalização e Reinicialização do PHP-FPM
            sudo systemctl restart php8.3-fpm
            sudo rm /tmp/erp_monolito_deploy.zip
            
            echo "✅ Deploy do ERP Monolito Finalizado com Sucesso!"
          EOF